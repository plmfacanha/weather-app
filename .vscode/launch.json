{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Webpack (Firefox)",
      "type": "firefox",
      "request": "launch",
      "url": "http://localhost:8080",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
